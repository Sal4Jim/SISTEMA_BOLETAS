<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MYPK2"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.MYPK2.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Nueva actividad para ver toda la carta -->
        <activity
            android:name=".CartaActivity"
            android:theme="@style/Theme.MYPK2.NoActionBar"
            android:exported="false" />

        <!-- Nueva actividad para ver solo el menú disponible -->
        <activity
            android:name=".MenuActivity"
            android:theme="@style/Theme.MYPK2.NoActionBar"
            android:exported="false" />

        <!-- Mantener OrdenActivity (ahora no se usa desde Home pero puedes mantenerla) -->
        <activity
            android:name=".OrdenActivity"
            android:theme="@style/Theme.MYPK2.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".EditarMenuActivity"
            android:theme="@style/Theme.MYPK2.NoActionBar"
            android:exported="false" />

    </application>

</manifest>